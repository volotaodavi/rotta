/**
 * PLACEHOLDER — nao e a tela real do Admin Rotta.
 *
 * A implementacao completa (clientes, planos, financeiro, suporte,
 * logs, metricas) esta especificada no Dossie 11, Secao 6, e no Dossie
 * 20 (`ADM-*`). Este arquivo existe apenas para que a rota "/" seja
 * valida e o app compile/rode.
 */
export default function AdminHomePlaceholder(): JSX.Element {
  return (
    <div className="flex min-h-[80vh] flex-col items-center justify-center gap-2 text-center">
      <h1 className="text-2xl font-semibold text-text">Admin Rotta</h1>
      <p className="text-text-muted">Painel interno — em construção.</p>
    </div>
  );
}
