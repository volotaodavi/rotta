import { Module } from "@nestjs/common";

/**
 * Modulo Support (Dossie 13, Secao 16) — canal de suporte (tickets,
 * mensagens) entre tenants e a equipe Rotta.
 *
 * ESTADO ATUAL: modulo vazio (fase de fundacao).
 */
@Module({})
export class SupportModule {}
