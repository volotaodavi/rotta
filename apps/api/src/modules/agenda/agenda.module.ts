import { Module } from "@nestjs/common";

/**
 * Modulo Agenda (Dossie 13, Secao 14) — calendario unificado: feriados,
 * recessos, eventos escolares, trocas pontuais, ausencias planejadas,
 * manutencoes, vencimentos derivados.
 *
 * ESTADO ATUAL: modulo vazio (fase de fundacao).
 */
@Module({})
export class AgendaModule {}
