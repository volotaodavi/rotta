import { Module } from "@nestjs/common";

/**
 * Modulo Drivers (Dossie 13, Secao 5) — cadastro, documentacao (CNH,
 * EAR, cursos), disponibilidade e status derivado dos motoristas.
 *
 * ESTADO ATUAL: modulo vazio (fase de fundacao).
 */
@Module({})
export class DriversModule {}
