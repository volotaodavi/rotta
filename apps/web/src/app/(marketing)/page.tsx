/**
 * PLACEHOLDER — nao e a Landing Page real.
 *
 * A implementacao completa (hero, beneficios, como funciona, para quem
 * e, depoimentos, FAQ, CTA, rodape) esta especificada no Dossie 11,
 * Secao 1, e sera construida a partir dos componentes de `@rotta/ui`
 * quando esta fase de fundacao for concluida. Este arquivo existe apenas
 * para que a rota "/" seja valida e o app compile/rode.
 */
export default function LandingPagePlaceholder(): JSX.Element {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 px-6 text-center">
      <h1 className="text-4xl font-bold text-text">Rotta</h1>
      <p className="text-text-muted">
        Gestão inteligente para transporte escolar — em construção.
      </p>
    </main>
  );
}
