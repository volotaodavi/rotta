/**
 * Declaração de tipo pro import de `icon.png` (`auth-logo-mark.tsx`) —
 * mesmo motivo/padrão de `welcome-hero.png.d.ts` (ver ali o porquê:
 * `moduleResolution: "bundler"` + `allowArbitraryExtensions` exigem um
 * `<nome>.<ext>.d.ts` ao lado do asset pra imports relativos com
 * extensão não-JS/TS).
 */
declare const value: number;
export default value;
