{
  "name": "@rotta/shared-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Fonte unica de verdade dos contratos de dominio (entidades, DTOs, eventos) — Dossie 22, Secao 6.3. Consumido por packages/types e packages/validators no frontend, e diretamente por apps/api no backend.",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[@rotta/shared-contracts] nenhum teste ainda\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@rotta/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.7.2"
  }
}
