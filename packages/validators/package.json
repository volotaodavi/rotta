{
  "name": "@rotta/validators",
  "version": "0.1.0",
  "private": true,
  "description": "Schemas de validacao (Zod) reutilizados por frontend e backend (Dossie 22 Secao 5.5) — a mesma regra escrita uma unica vez.",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[@rotta/validators] nenhum teste ainda\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@rotta/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.7.2"
  }
}
