/**
 * @rotta/notifications — cliente de push e notificacao in-app do frontend
 * (Dossie 22, Secao 5.12).
 *
 * Abstrai a diferenca entre a API de notificacao do navegador (web) e a
 * API nativa via Firebase (mobile, Dossie 9 Secao 2.7) — cada app
 * consumira uma interface unica de registro de token de dispositivo e de
 * exibicao de banner interno (Dossie 10 Secao 9.6), nunca a integracao de
 * plataforma diretamente.
 *
 * Nenhuma logica implementada ainda (fase de fundacao).
 */

export {};
