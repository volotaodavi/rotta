{
  "name": "@rotta/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente HTTP tipado consumido por apps/web, apps/mobile e apps/admin — nenhum app faz fetch/axios cru contra a Core API (Dossie 22 Secao 5.6).",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[@rotta/api-client] nenhum teste ainda\" && exit 0",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@rotta/types": "workspace:*"
  },
  "devDependencies": {
    "@rotta/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.7.2"
  }
}
