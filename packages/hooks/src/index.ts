/**
 * @rotta/hooks — hooks React reutilizaveis entre apps (web e mobile),
 * genericos o suficiente para nao pertencer a nenhum dominio de negocio
 * especifico (Dossie 22, Secao 5.8).
 *
 * Exemplos previstos (Dossie 22, Secao 5.8), a implementar quando a
 * primeira feature real precisar deles: `useDebounce`, `useMediaQuery`,
 * `useGeolocation` (abstraido por plataforma).
 *
 * Hooks de dominio (ex. `useAlunoAtivo`) NAO vao aqui — vivem dentro da
 * pasta `features/` do app que os usa (Dossie 23, Secao 1.1).
 */

export {};
