{
  "name": "@rotta/theme",
  "version": "0.1.0",
  "private": true,
  "description": "Tokens de design da Rotta (cores, tipografia, espacamento, raio, elevacao, motion) — fonte unica de verdade consumida por @rotta/ui/web e @rotta/ui/native (Dossie 10 e Dossie 22, Secao 5.2).",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens/*": "./src/tokens/*.ts"
  },
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[@rotta/theme] nenhum teste ainda — ver Dossie 23, Secao 10\" && exit 0",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@rotta/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.7.2"
  }
}
