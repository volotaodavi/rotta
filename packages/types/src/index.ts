/**
 * @rotta/types — tipos TypeScript de todo contrato de dominio.
 *
 * Nao escreva tipos manualmente em duplicidade aqui: este pacote
 * re-exporta tipos derivados de `shared/contracts` (a fonte canonica dos
 * schemas de DTO/evento, Dossie 22 Secao 6.3). Ate a primeira entidade de
 * dominio ser modelada (fora de escopo desta fase de fundacao — ver
 * Dossie 8), as pastas abaixo existem apenas como organizacao:
 *
 *   entities/   Tipos que espelham as entidades do Dossie 8 (Aluno, Rota...)
 *   dtos/       Tipos de request/response de cada endpoint do Dossie 13
 *   events/     Tipos dos eventos de dominio do Dossie 14, Secao 4
 */

export {};
