/**
 * @rotta/forms — logica e componentes de formulario reutilizavel
 * (Dossie 22, Secao 5.10), usados por qualquer fluxo de cadastro (Dossie
 * 15-20) em qualquer app. Integra `@rotta/validators` (schema) e
 * `@rotta/ui` (campo com label + erro + helper, Dossie 10 Secao 9.2).
 *
 * Nenhum componente implementado ainda (fase de fundacao).
 */

export {};
